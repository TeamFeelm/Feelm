import "styled-components";

// 스타일드 컴퍼넌트 타입 인덱스 파일임.
// 이 파일의 원래 용도는
// 1. 선언된 전체 타입 정리해놓는 곳
// 2. 여기서 선언된 타입은 글로벌하게 사용할 수 있도록 설정도 가능 (export 불필요)

// 히지만 지금은 타입이 길어질 경우만 여기서 작성하고
// export 해가는 걸로.

/*

declare module "styled-components" {
  export interface 타입명 {
    키1 : 밸류1,
    키2 : 밸류2,
    ...
  }

} */
